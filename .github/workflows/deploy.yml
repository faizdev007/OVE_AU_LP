# name: FTP Deploy

# on:
#   push:
#     branches:
#       - main

# jobs:
#   ftp-deploy:
#     name: Upload files via FTP
#     runs-on: ubuntu-latest

#     steps:
#       - name: 🧾 Checkout code
#         uses: actions/checkout@v3

#       - name: 📂 FTP Deploy
#         uses: SamKirkland/FTP-Deploy-Action@v4.3.4
#         with:
#           server: ${{ secrets.FTP_SERVER }}
#           username: ${{ secrets.FTP_USERNAME }}
#           password: ${{ secrets.FTP_PASSWORD }}
#           local-dir: ./ # Change to your build directory if needed (e.g., `dist/`, `public/`)
#           server-dir: /public_html/ # FTP root directory path
#           exclude: |
#             **/.git*
#             **/.github*
#             **/node_modules/*
#             **/.env
